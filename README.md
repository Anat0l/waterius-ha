# Waterius Home Assistant Integration

[![hacs_badge](https://img.shields.io/badge/HACS-Custom-orange.svg)](https://github.com/custom-components/hacs)

## Описание

Интеграция **Waterius** для Home Assistant позволяет подключать и управлять счетчиками воды, газа, электроэнергии и других ресурсов от производителя Waterius. Интеграция работает по принципу **push-based** модели: устройства сами отправляют данные на Home Assistant через HTTP POST запросы, что обеспечивает надежную и эффективную передачу данных без необходимости постоянного опроса устройств.

### Основные возможности

- 🔌 **Автоматическое подключение устройств** - устройства Waterius автоматически отправляют данные на Home Assistant
- 📊 **Мониторинг двух каналов** - поддержка двух независимых каналов измерения (например, холодная и горячая вода)
- 🔋 **Мониторинг состояния** - отслеживание уровня батареи, напряжения и состояния устройства
- 🎛️ **Гибкая настройка** - настройка типов счетчиков, коэффициентов преобразования и единиц измерения для каждого канала
- 📈 **Динамические свойства сенсоров** - автоматическое изменение названия, иконки, единиц измерения (м³, кВт·ч, Гкал) и device_class в зависимости от типа ресурса
- 🔄 **Синхронизация настроек** - сенсор `config_sync` отслеживает соответствие желаемых настроек с реальным состоянием устройства
- 🌐 **Локальная работа** - все данные обрабатываются локально, без отправки в облако
- 🔄 **Автоматическое добавление устройств** - новые устройства могут быть автоматически добавлены при первом подключении
- 📝 **Подробная диагностика** - множество диагностических сенсоров для мониторинга работы устройств
- 🌍 **Переводимые исключения** - все ошибки переведены на русский и английский языки для удобства диагностики

### Как это работает

1. **Устройства Waterius** периодически отправляют данные измерений на Home Assistant через HTTP POST запросы на встроенный веб-сервер
2. **Веб-сервер интеграции** (на базе `HomeAssistantView`) принимает и валидирует данные
3. **Интеграция автоматически** идентифицирует устройство по MAC адресу или ключу и обновляет соответствующие сенсоры
4. **Сенсоры обновляются** в реальном времени с динамическими свойствами (название, иконка, единицы измерения) в зависимости от выбранного типа ресурса
5. **Данные доступны** через стандартные сенсоры Home Assistant для использования в автоматизациях, дашбордах и уведомлениях
6. **Сенсор синхронизации** отслеживает соответствие желаемых настроек (select/number) с текущим состоянием устройства

### Для кого предназначена

Интеграция идеально подходит для:
- Владельцев счетчиков Waterius, которые хотят интегрировать их в систему умного дома
- Пользователей, желающих автоматизировать учет потребления ресурсов (вода, газ, электричество)
- Тех, кому нужен локальный контроль без зависимости от облачных сервисов
- Пользователей, которым требуется гибкая настройка типов счетчиков и единиц измерения

### Поддерживаемые устройства

#### Устройство Waterius

Интеграция разработана для работы с устройством **Waterius** ([waterius.ru](https://waterius.ru)) - умным контроллером для передачи показаний счетчиков в Home Assistant.

**Технические характеристики:**
- 📡 **Связь:** WiFi 2.4 GHz (802.11 b/g/n)
- 🔌 **Интерфейс:** HTTP POST API
- 🔋 **Питание:** 3× AA батареи (Li-Ion или щелочные)
- 📊 **Каналы:** 2 независимых канала для счетчиков
- 🎛️ **Типы счетчиков:** Механические, электронные (с импульсным выходом)
- 🌐 **Протокол:** Локальная push-based модель (без облака)

#### Совместимые счетчики

Waterius совместим с **большинством импульсных счетчиков** воды, газа, электроэнергии и тепла, оснащенных механическими цифрами и проводом для передачи импульсов.

**Рекомендованные модели счетчиков воды:**
- **Decast (Декаст)** ВСКМ-15
- **Valtec** VLF-15U-I
- **Пульсар**
- **Itelma (Ителма)** WFK23, WFW23, WFK24, WFW24
- **Meter (Метер)** СВУ-15
- **Бетар** СВГ-15
- **Карат-140**
- **ZENNER** ETK-i, ETW-i, MTK-i, MTW-i
- **Minomess (Minol)** FAZ
- **Elster** S100
- **Enbra** EV-AMi, EV-AM1i
- **Maddalena** CDS/DS
- **Gerrida, Экомера, Эконом**
- **Водомер** ОВСУ-15
- **Охта Тайпит, Пульс Pulse**
- **Тепловодомер** ВСГд-15
- **Алексеевский**
- **Аршин** СВУ-15-80И, СВУ-15-110И
- **Берегун, Водомеръ**
- **Мультисистема, Норма**

**Счетчики газа, электроэнергии и тепла:**
- Любые счетчики с импульсным выходом
- Механические и электронные счетчики
- Требуется наличие провода для передачи импульсов

**Важно:** Для некоторых моделей необходимо соблюдать полярность при подключении. Подробная информация о подключении конкретных моделей доступна в [базе знаний Waterius](https://faq.waterius.ru).

### Поддерживаемые функции и entities

Интеграция предоставляет **60 entities** через 4 платформы Home Assistant.

#### 📊 Платформы

- **Sensor** (Сенсоры) - 52 entities
- **Select** (Выбор) - 6 entities  
- **Number** (Число) - 1 entity
- **Switch** (Переключатель) - 1 entity

#### 🎯 Основные сенсоры

| Entity | Название | Тип | Описание |
|--------|----------|-----|----------|
| `ch0` | Канал 0 | Sensor | Текущие показания счетчика канала 0 (динамический тип: вода/газ/электричество) |
| `ch1` | Канал 1 | Sensor | Текущие показания счетчика канала 1 (динамический тип: вода/газ/электричество) |
| `delta0` | Дельта 0 | Sensor | Изменение показаний канала 0 с последней передачи |
| `delta1` | Дельта 1 | Sensor | Изменение показаний канала 1 с последней передачи |

#### ⚙️ Конфигурационные entities

| Entity | Название | Тип | Описание |
|--------|----------|-----|----------|
| `channel_0_data_type` | Канал 0 - Тип счетчика | Select | Механический / Электронный / Не используется |
| `channel_1_data_type` | Канал 1 - Тип счетчика | Select | Механический / Электронный / Не используется |
| `channel_0_data_type_data` | Канал 0 - Учет данных | Select | Холодная вода / Горячая вода / Газ / Электричество / Тепло / Другое |
| `channel_1_data_type_data` | Канал 1 - Учет данных | Select | Холодная вода / Горячая вода / Газ / Электричество / Тепло / Другое |
| `channel_0_conversion_factor` | Канал 0 - Коэффициент пересчета | Select | 1 / 10 / 100 |
| `channel_1_conversion_factor` | Канал 1 - Коэффициент пересчета | Select | 1 / 10 / 100 |
| `period_min_config` | Интервал обновления данных | Number | Период отправки данных в минутах (1-1440) |
| `send_settings` | Отправить настройки | Switch | Переключатель для отправки настроек на устройство |
| `config_sync` | Синхронизация настроек | Sensor | Статус синхронизации настроек (синхронизированы / не синхронизированы) |

#### 📡 Диагностические сенсоры

**Каналы и счетчики (12 сенсоров):**
- `ch0_start`, `ch1_start` - Начальные значения каналов
- `imp0`, `imp1` - Количество импульсов от счетчиков
- `adc0`, `adc1` - Значения ADC каналов
- `ctype0`, `ctype1` - Текущий тип счетчика канала
- `cname0`, `cname1` - Текущее имя (тип данных) канала
- `data_type0`, `data_type1` - Тип данных канала (устаревший)
- `f0`, `f1` - Коэффициент пересчета канала
- `serial0`, `serial1` - Серийные номера счетчиков

**Питание и батарея (4 сенсора):**
- `battery` - Уровень заряда батареи (%)
- `voltage` - Напряжение (В)
- `voltage_low` - Низкое напряжение (да/нет)
- `voltage_diff` - Разница напряжений (В)

**Сеть WiFi (9 сенсоров):**
- `rssi` - Уровень сигнала WiFi (dBm)
- `channel` - Канал WiFi (1-14)
- `wifi_phy_mode` - Режим WiFi PHY (B/G/N)
- `wifi_phy_mode_s` - Режим WiFi PHY (строка)
- `router_mac` - MAC адрес роутера
- `ip` - IP адрес устройства
- `dhcp` - DHCP включен (да/нет)
- `mac` - MAC адрес устройства
- `wifi_connect_errors` - Ошибки подключения WiFi

**Система и устройство (8 сенсоров):**
- `version` - Версия прошивки
- `version_esp` - Версия ESP
- `esp_id` - ID ESP чипа
- `flash_id` - ID Flash памяти
- `freemem` - Свободная память (байты)
- `email` - Email пользователя
- `company` - Компания
- `place` - Место установки

**Время и таймеры (5 сенсоров):**
- `timestamp` - Время последней передачи
- `waketime` - Время пробуждения (мс)
- `setuptime` - Время настройки (мс)
- `period_min` - Период отправки данных (мин)
- `period_min_tuned` - Настроенный период (мин)

**Состояние и события (6 сенсоров):**
- `boot` - Количество загрузок
- `resets` - Количество перезагрузок
- `mode` - Режим работы
- `setup_started` - Настроек начато
- `setup_finished` - Настроек завершено
- `ntp_errors` - Ошибки синхронизации времени

**Протоколы (4 сенсора):**
- `mqtt` - MQTT включен (да/нет)
- `ha` - Home Assistant интеграция (да/нет)
- `http` - HTTP включен (да/нет)
- `zeroconf_status` - Статус Zeroconf регистрации

#### 🎨 Динамические свойства

Некоторые entities имеют **динамические свойства**, которые изменяются в зависимости от выбранного типа ресурса:

**Для сенсоров `ch0` и `ch1`:**
- **Единицы измерения**: м³ (вода/газ), кВт·ч (электричество/тепло), Гкал (тепло)
- **Device class**: `water`, `gas`, `energy` или `None`
- **Иконка**: автоматически меняется на иконку ресурса (вода/газ/электричество/тепло)
- **Название**: автоматически меняется на тип ресурса (например, "Холодная вода", "Электричество")
- **Обновление в реальном времени**: все свойства обновляются сразу при изменении select entity, не дожидаясь данных от устройства

**Для сенсоров `rssi` и `voltage_low`:**
- **Иконка**: динамически меняется в зависимости от значения (через `icons.json`)

#### 🔒 Скрытые по умолчанию

**43 диагностических сенсора** скрыты по умолчанию для упрощения интерфейса. Они могут быть активированы пользователем при необходимости через настройки entities в Home Assistant.

### Типы поддерживаемых ресурсов

Интеграция поддерживает следующие типы ресурсов:
- 💧 **Вода** (холодная, горячая, питьевая)
- ⚡ **Электроэнергия**
- 🔥 **Газ**
- 🌡️ **Тепло** (Гкал, кВт)
- 📦 **Другие** (настраиваемые типы)

Каждый канал может быть настроен независимо с выбором типа ресурса, типа счетчика (механический/электронный) и коэффициента преобразования.

## Установка

### Через HACS (рекомендуется)

1. Откройте HACS в Home Assistant
2. Перейдите в раздел "Интеграции"
3. Нажмите на три точки в правом верхнем углу
4. Выберите "Пользовательские репозитории"
5. Добавьте URL: `https://github.com/Anat0l/waterius-ha`
6. Выберите категорию: "Integration"
7. Найдите "Waterius" в списке и установите
8. Перезапустите Home Assistant
9. Перейдите в Настройки → Устройства и службы → Добавить интеграцию
10. Найдите "Waterius" и следуйте инструкциям настройки

### Ручная установка

1. Скопируйте папку `custom_components/waterius_ha` в директорию `custom_components` вашего Home Assistant
2. Перезапустите Home Assistant
3. Перейдите в Настройки → Устройства и службы → Добавить интеграцию
4. Найдите "Waterius" и следуйте инструкциям настройки

## Настройка

При настройке интеграции вы можете указать:
- **Автоматическое добавление устройств** - автоматически добавлять устройства при получении запросов от неизвестных устройств (по умолчанию выключено)

### Добавление устройств

После первоначальной настройки интеграции вы можете добавить устройства:

1. Перейдите в Настройки → Устройства и службы
2. Найдите интеграцию "Waterius" и нажмите на неё
3. Нажмите на кнопку "Настроить" (три точки в правом верхнем углу)
4. Выберите "Добавить устройство"
5. Введите:
   - **ID устройства** - уникальный идентификатор устройства (например, `waterius_kitchen`)
   - **Имя устройства** - понятное имя для отображения (например, `Счетчик воды кухня`)
   - **MAC адрес** (опционально) - MAC адрес устройства из JSON данных (например, `11:22:33:44:55:66`)
6. Нажмите "Отправить"

**Важно:** MAC адрес используется для автоматической идентификации устройства при получении данных. Если MAC адрес не указан, устройство можно будет идентифицировать только по device_id вручную.

Вы можете добавить несколько устройств. Для удаления устройства выберите "Удалить устройство" в меню настроек.

### Настройки интеграции

В меню настроек интеграции (опция "Настройки интеграции") вы можете:
- **Включить/выключить автоматическое добавление устройств** - управлять автоматическим добавлением новых устройств

### Управление неиспользуемыми каналами

Если вы используете только один канал счетчика, вы можете скрыть неиспользуемый канал:

1. Откройте настройки устройства
2. Найдите селект "Канал X - (01) Тип счетчика"
3. Выберите **"Не используется"**

**Что произойдет:**
- ✅ Все связанные entities канала автоматически **отключатся**:
  - Сенсор `channel_X` (показания счетчика)
  - Селект "Тип данных" (вода, газ, электричество)
  - Селект "Коэффициент пересчета"
- 📝 UI станет чище и покажет только активные каналы
- 🔄 При изменении обратно на "Механический" или "Электронный", все entities **автоматически включатся**

**Важно:**
- Если вы вручную отключили какой-то entity, интеграция не будет его автоматически включать
- Интеграция управляет только теми entities, которые она сама отключила
- Ваш ручной выбор всегда уважается


### Автоматическое добавление устройств

Когда включено автоматическое добавление устройств, интеграция будет автоматически добавлять новые устройства при получении запросов от неизвестных устройств:

- **ID устройства** генерируется автоматически на основе MAC адреса (формат: `waterius_xx_xx_xx_xx_xx_xx`)
- **Имя устройства** генерируется автоматически:
  - Если в данных есть серийный номер (`serial0` или `serial1`), используется формат: `Waterius {серийный_номер}`
  - Иначе используется формат: `Waterius {последние_4_символа_MAC}`
- **MAC адрес** берется из данных запроса
- Устройство автоматически сохраняется в конфигурацию интеграции

**Важно:** Автоматически добавленные устройства можно впоследствии переименовать или удалить через настройки интеграции.

### Настройка устройства Waterius

Для работы с интеграцией необходимо настроить устройство Waterius на отправку данных на Home Assistant:

1. **Подключитесь к веб-интерфейсу Waterius** (обычно http://192.168.4.1 в режиме AP или IP устройства в вашей сети)

2. **Настройте эндпоинт для отправки данных:**
   - **URL**: `http://YOUR_HA_IP:8123/api/waterius`
   - Замените `YOUR_HA_IP` на IP адрес вашего Home Assistant
   - Замените `8123` на порт вашего Home Assistant, если вы его изменили

3. **Настройте эндпоинт для получения настроек (опционально):**
   - **URL**: `http://YOUR_HA_IP:8123/api/waterius/cfg`
   - Этот эндпоинт используется устройством для получения конфигурации счетчиков

4. **Сохраните настройки** и перезагрузите устройство

**Примечания:**
- Интеграция использует стандартный HTTP (не HTTPS) из-за ограничений устройства
- Убедитесь, что устройство Waterius и Home Assistant находятся в одной локальной сети
- Устройство автоматически идентифицируется по MAC адресу из JSON данных

### Работа с устройствами

Интеграция автоматически определяет устройство по MAC адресу из поля `mac` в JSON данных. Устройство должно быть предварительно добавлено через настройки интеграции с указанием MAC адреса.

При добавлении устройства:
- **ID устройства** - уникальный идентификатор (например, `waterius_kitchen`)
- **Имя устройства** - понятное имя (например, `Счетчик воды кухня`)
- **MAC адрес** (опционально) - MAC адрес устройства из JSON (например, `11:22:33:44:55:66`)

Если MAC адрес указан, интеграция автоматически свяжет входящие данные с соответствующим устройством.

Все данные из POST запросов будут доступны через события в Home Assistant:
- `waterius_data_received` - для запросов от неизвестных устройств
- `waterius_device_data_received` - для запросов от зарегистрированных устройств
- `waterius_device_update` - при обновлении данных устройства

## Обновление данных

Интеграция Waterius использует **push-based модель** обновления данных, что означает, что устройства сами инициируют отправку данных, а не опрашиваются Home Assistant. Это обеспечивает эффективное использование ресурсов и надежную передачу данных.

### Как происходит обновление данных

#### 1. Отправка данных устройством

Устройства Waterius периодически отправляют данные измерений на Home Assistant через HTTP POST запросы. Частота отправки данных настраивается на самом устройстве (параметр `period_min` в минутах, обычно от 60 до 1440 минут - сутки).

**Процесс отправки:**
- Устройство собирает данные измерений (значения каналов, напряжение, батарея и т.д.)
- Устройство формирует JSON объект с данными
- Устройство отправляет POST запрос на IP адрес Home Assistant и стандартный порт (по умолчанию 8123)
- Запрос отправляется на эндпоинт: `/api/waterius`

#### 2. Прием данных веб-сервером

Веб-сервер интеграции, работающий внутри Home Assistant, принимает входящие POST запросы:

**Процесс приема:**
- Веб-сервер интеграции зарегистрирован в HTTP компоненте Home Assistant на пути `/api/waterius`
- При получении POST запроса веб-сервер извлекает JSON данные из тела запроса
- Валидируется формат JSON и наличие обязательных полей (размер до 5KB)
- Извлекается MAC адрес устройства из поля `mac` в JSON данных для идентификации

#### 3. Идентификация устройства

Интеграция автоматически идентифицирует устройство по MAC адресу:

**Процесс идентификации:**
- Из JSON данных извлекается MAC адрес (например, `11:22:33:44:55:66`)
- MAC адрес нормализуется (приводится к верхнему регистру)
- Интеграция ищет зарегистрированное устройство с таким MAC адресом
- Если устройство найдено, используется его `device_id` и `device_name`
- Если устройство не найдено и включено автоматическое добавление устройств, создается новое устройство

#### 4. Обновление данных устройства

После идентификации устройства происходит обновление его данных:

**Процесс обновления:**
- Все данные из JSON запроса сохраняются в объект устройства (`WateriusDevice.data`)
- Данные включают:
  - Значения каналов (`ch0`, `ch1`)
  - Дельты (`delta0`, `delta1`)
  - Состояние питания (`voltage`, `voltage_low`, `battery`)
  - Сетевую информацию (`ip`, `mac`, `channel`, `rssi`)
  - Информацию о прошивке (`version`, `version_esp`)
  - Системную информацию (`timestamp`, `freemem`, `mode`)
  - И другие диагностические данные

#### 5. Обновление entities (сенсоров, селектов, number)

После обновления данных устройства автоматически обновляются все связанные entities:

**Процесс обновления entities:**

1. **Генерация события `waterius_device_update`:**
   - DeviceManager отправляет событие в Home Assistant event bus
   - Событие содержит `device_id`, `device_name` и полные `data`

2. **Подписка entities на события:**
   - Все entities (сенсоры, селекты, number) подписаны на событие `waterius_device_update`
   - При получении события entities проверяют, относится ли оно к их устройству

3. **Обновление состояния entities:**
   - **Сенсоры (sensors):** Обновляют свои значения (`native_value`) из `device.data`
   - **Селекты (selects):** Обновляют текущую опцию из `device.data` (если значение изменилось)
   - **Number:** Обновляют значение из `device.data` (например, `period_min`)

4. **Динамические свойства:**
   - Для сенсоров `ch0`, `ch1`, `ch0_start`, `ch1_start` динамически обновляются:
     - Единицы измерения (`native_unit_of_measurement`) - м³, кВт·ч и т.д.
     - Класс устройства (`device_class`) - WATER, ENERGY и т.д.
     - Имя сенсора (для `ch0` и `ch1`) - на основе выбранного типа ресурса

5. **Обновление в Home Assistant:**
   - Entities вызывают `async_write_ha_state()` для обновления состояния в Home Assistant
   - Home Assistant автоматически сохраняет историю изменений
   - Обновления отображаются в интерфейсе в реальном времени

#### 6. Генерация событий

Интеграция генерирует несколько типов событий для различных сценариев:

**События:**

- **`waterius_device_data_received`** - генерируется при получении данных от зарегистрированного устройства
  - Содержит: `device_id`, `device_name`, `mac`, `data`, `remote`
  - Используется для автоматизаций, которые реагируют на получение данных

- **`waterius_data_received`** - генерируется при получении данных от неизвестного устройства
  - Содержит: `path`, `data`, `mac`, `device_id`, `remote`
  - Используется для обработки данных от устройств, которые еще не зарегистрированы

- **`waterius_device_update`** - генерируется при обновлении данных устройства
  - Содержит: `device_id`, `device_name`, `data`
  - Используется entities для обновления своего состояния

#### 7. Логирование данных

Если включено логирование в настройках интеграции, все данные от устройств записываются в журнал Home Assistant:

**Логируемая информация:**
- Имя устройства
- Значения каналов (CH0, CH1)
- Значения дельты (Δ0, Δ1)
- Напряжение и статус низкого напряжения
- Уровень батареи
- Время получения данных
- IP адрес устройства

### Частота обновлений

**Частота обновлений зависит от настроек устройства:**
- Устройства Waterius отправляют данные с интервалом, заданным параметром `period_min` (в минутах)
- Типичные значения: 5, 10, 15, 30, 60 минут
- Интервал можно настроить через сенсор `period_min` в Home Assistant

**Важно:**
- Интеграция не опрашивает устройства - она только принимает данные
- Если устройство не отправляет данные, entities останутся в последнем известном состоянии
- Свойство `available` для entities основано на наличии устройства в менеджере, а не на времени последнего обновления

### Особенности обновления

**Автоматическое обновление:**
- Все обновления происходят автоматически при получении данных от устройства
- Не требуется настройка интервалов опроса или таймеров
- Обновления происходят в реальном времени при получении POST запроса

**Сохранение состояния:**
- Entities используют `RestoreEntity` для восстановления последнего состояния после перезапуска Home Assistant
- Значения селектов и number сохраняются между перезапусками
- История данных сохраняется в базе данных Home Assistant

**Обработка ошибок:**
- Если POST запрос содержит невалидный JSON, веб-сервер возвращает ошибку 400
- Если устройство не найдено и автоматическое добавление выключено, данные логируются, но не обрабатываются
- Если устройство найдено, но данные не обновляются, ошибка логируется, но не прерывает работу интеграции

### Пример использования

#### POST запрос с JSON данными:

Отправьте POST запрос с JSON телом на эндпоинт интеграции:
```
POST http://your-home-assistant-ip:8123/api/waterius
Content-Type: application/json

{
  "delta0": 0,
  "delta1": 95320,
  "ch0": 0,
  "ch1": 262,
  "voltage": 3.207,
  "voltage_low": true,
  "battery": 0,
  "channel": 11,
  "mac": "11:22:33:44:55:66",
  "ip": "192.168.1.100",
  "version": 32,
  "timestamp": "2025-11-23T14:52:31+0000"
}
```

**Эндпоинты интеграции:**
- **`/api/waterius`** - POST для отправки данных измерений от устройств
- **`/api/waterius/cfg`** - POST/GET для получения настроек устройствами (конфигурация счетчиков)

Если устройство с MAC адресом `11:22:33:44:55:66` зарегистрировано в интеграции, будет создано событие `waterius_device_data_received`:
```json
{
  "device_id": "waterius_kitchen",
  "device_name": "Счетчик воды кухня",
  "mac": "11:22:33:44:55:66",
  "data": {
    "delta0": 0,
    "delta1": 95320,
    "ch0": 0,
    "ch1": 262,
    "voltage": 3.207,
    "voltage_low": true,
    "battery": 0,
    "channel": 11,
    "mac": "11:22:33:44:55:66",
    "ip": "192.168.1.100",
    "version": 32,
    "timestamp": "2025-11-23T14:52:31+0000"
  },
  "remote": "192.168.1.100"
}
```

Если устройство не найдено, будет создано событие `waterius_data_received` с полными данными.

#### Пример с использованием curl:

```bash
# Отправка данных измерений
curl -X POST http://your-home-assistant-ip:8123/api/waterius \
  -H "Content-Type: application/json" \
  -d '{
    "delta0": 0,
    "delta1": 95320,
    "ch0": 0,
    "ch1": 262,
    "mac": "11:22:33:44:55:66",
    "voltage": 3.207,
    "timestamp": "2025-11-23T14:52:31+0000"
  }'

# Получение настроек устройством
curl -X POST http://your-home-assistant-ip:8123/api/waterius/cfg \
  -H "Content-Type: application/json" \
  -d '{
    "mac": "11:22:33:44:55:66",
    "key": "YOUR_DEVICE_KEY"
  }'
```

**Примечания:**
- Замените `your-home-assistant-ip` на реальный IP адрес вашего Home Assistant
- Замените `8123` на ваш порт Home Assistant, если вы изменили его
- Для эндпоинта `/api/waterius/cfg` устройство получит JSON с настройками (если переключатель "Отправить настройки" включен)

### Автоматизация в Home Assistant

Вы можете создать автоматизацию, которая реагирует на события устройств:

```yaml
automation:
  - alias: "Обработка данных Waterius устройства"
    trigger:
      platform: event
      event_type: waterius_device_data_received
    condition:
      condition: template
      value_template: "{{ trigger.event.data.device_id == 'waterius_kitchen' }}"
    action:
      - service: logbook.log
        data:
          name: Waterius
          message: >
            Устройство {{ trigger.event.data.device_name }}:
            Канал 0: {{ trigger.event.data.data.ch0 }},
            Канал 1: {{ trigger.event.data.data.ch1 }},
            Напряжение: {{ trigger.event.data.data.voltage }}V
      
  - alias: "Уведомление о низком напряжении"
    trigger:
      platform: event
      event_type: waterius_device_data_received
    condition:
      condition: template
      value_template: "{{ trigger.event.data.data.voltage_low == true }}"
    action:
      - service: notify.mobile_app
        data:
          message: "Низкое напряжение на устройстве {{ trigger.event.data.device_name }}"
```

## Удаление интеграции

Если вы хотите удалить интеграцию Waterius из Home Assistant, выполните следующие шаги:

### Способ 1: Через интерфейс Home Assistant (рекомендуется)

1. Перейдите в **Настройки** → **Устройства и службы**
2. Найдите интеграцию **"Waterius"** в списке
3. Нажмите на интеграцию, чтобы открыть её настройки
4. Нажмите на три точки (⋮) в правом верхнем углу
5. Выберите **"Удалить"** или **"Удалить интеграцию"**
6. Подтвердите удаление в появившемся диалоговом окне

После удаления:
- Все устройства Waterius будут удалены из Home Assistant
- Все сенсоры, селекты и настройки будут удалены
- История данных будет сохранена в базе данных Home Assistant (если вы хотите полностью удалить данные, используйте способ 2)

### Способ 2: Полное удаление (включая файлы)

Если вы хотите полностью удалить интеграцию, включая файлы:

1. **Удалите интеграцию через интерфейс** (см. Способ 1)
2. **Удалите файлы интеграции:**
   - Перейдите в директорию `custom_components` вашего Home Assistant
   - Удалите папку `wateruis_ha`
3. **Перезапустите Home Assistant**
4. **(Опционально) Очистите базу данных:**
   - Если вы хотите удалить всю историю данных устройств Waterius, используйте функцию очистки истории в Home Assistant
   - Настройки → История и журнал событий → Очистить историю

### Важные замечания

- ⚠️ **Резервное копирование:** Перед удалением рекомендуется создать резервную копию Home Assistant, если вы хотите сохранить историю данных
- 📊 **История данных:** История показаний счетчиков будет сохранена в базе данных Home Assistant даже после удаления интеграции
- 🔄 **Повторная установка:** Вы можете установить интеграцию снова в любое время, но устройства нужно будет добавить заново
- 🗑️ **Автоматическая очистка:** При удалении интеграции все связанные entities (сенсоры, селекты, настройки) будут автоматически удалены

### Устранение проблем при удалении

Если интеграция не удаляется через интерфейс:

1. **Проверьте, что веб-сервер остановлен:**
   - Интеграция должна быть выгружена перед удалением
   - Попробуйте перезапустить Home Assistant

2. **Удалите вручную через конфигурацию:**
   - Откройте файл `.storage/core.config_entries`
   - Найдите запись с `"domain": "wateruis_ha"`
   - Удалите эту запись (создайте резервную копию перед редактированием!)

3. **Очистите кэш:**
   - Удалите файлы `.storage/core.entity_registry` и `.storage/core.device_registry` (создайте резервную копию!)
   - Перезапустите Home Assistant

## Требования

- Home Assistant 2023.1.0 или новее
- Python 3.9+

## Зависимости

### Внешние зависимости

Интеграция использует минимальное количество внешних зависимостей:

#### aiohttp (>=3.8.0)
- **Назначение**: Асинхронный HTTP клиент/сервер фреймворк
- **Использование**: Обработка HTTP POST запросов от устройств Waterius
- **Лицензия**: Apache License 2.0
- **Репозиторий**: https://github.com/aio-libs/aiohttp
- **Документация**: https://docs.aiohttp.org/
- **PyPI**: https://pypi.org/project/aiohttp/

**Почему именно эта версия?**
- Версия 3.8.0+ обеспечивает улучшенную поддержку async/await
- Лучшая обработка WebSocket соединений
- Расширенные функции безопасности
- Необходима для корректной интеграции с HTTP компонентом Home Assistant

### Зависимости Home Assistant

Интеграция использует следующие компоненты Home Assistant:

#### http
- **Назначение**: HTTP компонент Home Assistant
- **Использование**: Регистрация HTTP views для приема данных от устройств
- **Тип**: Основной компонент

#### zeroconf
- **Назначение**: Zero-configuration networking (mDNS/DNS-SD)
- **Использование**: Объявление сервиса интеграции в локальной сети для обнаружения устройствами
- **Тип**: Основной компонент

### Философия минимальных зависимостей

Интеграция намеренно использует минимальное количество зависимостей:
- ✅ Только одна внешняя зависимость (aiohttp)
- ✅ Нет зависимостей от облачных сервисов
- ✅ Нет зависимостей от баз данных
- ✅ Вся обработка данных происходит локально
- ✅ Используются встроенные компоненты Home Assistant где это возможно

### Безопасность

**Валидация входных данных:**
- Все входящие данные валидируются с использованием пользовательских валидаторов в `validators.py`
- Установлен максимальный размер JSON (5KB)
- Санитизация строк для предотвращения инъекций
- Проверка типов для всех полей данных

**Сетевая безопасность:**
- Только локальная сеть (нет облачных зависимостей)
- Использует стандартный HTTP (ограничение устройства)
- Валидация данных на всех конечных точках

**Обработка ошибок:**
- Все исключения переведены на русский и английский языки
- 9 классов переводимых исключений для различных сценариев ошибок
- Поддержка плейсхолдеров для динамической подстановки значений в сообщения об ошибках
- Наследование от `HomeAssistantError` для корректной интеграции с HA

Подробная информация о зависимостях доступна в файле [DEPENDENCIES.md](DEPENDENCIES.md).

## Разработка

### Тестирование

Интеграция имеет полное покрытие тестами для config flow согласно [Home Assistant Integration Quality Scale](https://developers.home-assistant.io/docs/core/integration-quality-scale/checklist/).

#### Запуск тестов

```bash
# Linux/Mac
./run_tests.sh

# Windows PowerShell
.\run_tests.ps1

# Или напрямую через pytest
pip install -r requirements-test.txt
pytest tests/
```

#### Статус тестирования

| Компонент | Покрытие | Тестов | Статус |
|-----------|----------|--------|--------|
| **Config Flow** | 100% | 14 | ✅ Готово |
| **Validators** | 95% | 35 | ✅ Готово |
| **Другие модули** | 0% | 0 | 📋 План создан |

**Общее покрытие:** ~4% (цель: >95%)

**Планируется:**
- 📋 Достижение >95% покрытия
- 🎯 ~326 тестов (unit + integration)
- ⏳ Unit-тесты: validators, const, exceptions, helpers
- ⏳ Integration-тесты: требуется установка Home Assistant

#### Документация по тестам

- 📄 [`tests/README.md`](tests/README.md) - Руководство по запуску тестов

### Strict Typing

Интеграция использует строгую типизацию с помощью mypy:

#### Проверка типов

```bash
# Linux/Mac
./check_typing.sh

# Windows PowerShell
.\check_typing.ps1

# Или вручную
mypy custom_components/waterius_ha/
```

#### Конфигурация

Строгая типизация настроена в `mypy.ini`:
- ✅ Адаптирована для Home Assistant API
- ✅ Все функции имеют типовые аннотации
- ✅ Все переменные с комплексными типами аннотированы
- ✅ PEP 561 совместимость (включен `py.typed`)
- ✅ 77% снижение ошибок типизации (с 57 до 13)

#### Стандарты типизации

```python
# ✅ Правильно
def process_data(device_id: str, data: dict[str, Any]) -> bool:
    """Process device data."""
    return True

# ❌ Неправильно - отсутствуют аннотации
def process_data(device_id, data):
    return True
```

Подробная информация о типизации:
- 📄 [`mypy.ini`](mypy.ini) - Конфигурация mypy

### Система переводимых исключений

Интеграция использует систему переводимых исключений для удобной диагностики:

#### Классы исключений

- **WateriusError** - Базовое исключение интеграции
- **WateriusValidationError** - Ошибки валидации данных
- **InvalidMACAddressError** - Недопустимый MAC адрес
- **MissingRequiredFieldError** - Отсутствует обязательное поле
- **InvalidFieldTypeError** - Неверный тип поля
- **ValueOutOfRangeError** - Значение вне допустимого диапазона
- **InvalidEncodingError** - Недопустимая кодировка
- **InvalidJSONError** - Недопустимый JSON
- **InvalidRequestError** - Недопустимый запрос
- **ZeroconfAddressError** - Ошибка IPv4 адресов для Zeroconf
- **ZeroconfConversionError** - Ошибка преобразования адресов

#### Поддерживаемые языки

- 🇷🇺 Русский
- 🇬🇧 Английский

#### Пример использования

```python
# Исключение автоматически переводится на язык пользователя
raise InvalidMACAddressError("AA:BB:CC:INVALID")
# RU: "Недопустимый формат MAC адреса: AA:BB:CC:INVALID"
# EN: "Invalid MAC address format: AA:BB:CC:INVALID"
```

### Quality Scale

Интеграция следует [Home Assistant Integration Quality Scale](https://developers.home-assistant.io/docs/core/integration-quality-scale/checklist/):

| Уровень | Прогресс | Статус |
|---------|----------|--------|
| 🥉 **Bronze** | **95%** (18/19) | ✅ Готов! |
| 🥈 **Silver** | **80%** (8/10) | ✅ Хорошо |
| 🥇 **Gold** | **91%** (20/22) | ✅ Отлично! |
| 💎 **Platinum** | **67%** (2/3) | ✅ Хорошо |

**Общий прогресс:** **89%** (48/54) ✅

#### Недавние улучшения

- ✅ **exception-translations** - Система переводимых исключений (9 классов, 18 переводов)
- ✅ **async-dependency** - Полная поддержка async через встроенный веб-сервер HA
- ✅ **docs-supported-functions** - Полный список всех 60 entities и платформ
- ✅ **docs-supported-devices** - Детальный список поддерживаемых устройств
- ✅ **test-config-flow** - 100% покрытие config_flow (14 тестов)
- ✅ **strict-typing** - Строгая типизация с mypy (77% снижение ошибок)

Подробности: [Home Assistant Integration Quality Scale Checklist](https://developers.home-assistant.io/docs/core/integration-quality-scale/checklist/)

### Установка зависимостей для разработки

```bash
# Базовые зависимости
pip install -r requirements.txt

# Зависимости для тестирования
pip install -r requirements-test.txt

# Проверка типов
./check_typing.sh  # или check_typing.ps1 на Windows

# Запуск тестов
./run_tests.sh     # или run_tests.ps1 на Windows
```

## Владелец интеграции

Интеграция разработана и поддерживается **[@Anat0l](https://github.com/Anat0l)**.

- 📧 **GitHub**: [@Anat0l](https://github.com/Anat0l)
- 🔗 **Репозиторий**: [waterius-ha](https://github.com/Anat0l/waterius-ha)
- 🐛 **Отчеты об ошибках**: [Issues](https://github.com/Anat0l/waterius-ha/issues)

Если у вас есть вопросы, предложения или вы обнаружили ошибку, пожалуйста, создайте issue в репозитории проекта.

